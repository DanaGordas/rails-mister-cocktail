<div class="container mt-3">
  <p>
    <strong>Name:</strong>
    <%= @cocktail.name %>
  </p>

  <p><strong>Ingredients:</strong></p>
    <ul>
      <% @cocktail.doses.each do |dose|%>
        <li>
          <%= dose.description %>
          <%= dose.ingredient.name %>
          <%= link_to "🗑 Delete", dose_path(dose), method: :delete %>
        </li>
      <% end %>
    </ul>

  <button type="button" class="btn btn-light p-3"><%= link_to "Add new ingredient", new_cocktail_dose_path(@cocktail) %></button>
  <br>
  <br>
  <button type="button" class="btn btn-light p-3"><%= link_to 'Back', cocktails_path %></button>
  <br>

</div>
