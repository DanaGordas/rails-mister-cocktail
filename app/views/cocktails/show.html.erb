<div class="container mt-3">

<% if @cocktail.photo.attached? %>
  <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path @cocktail.photo.key %>')">
    <%= @cocktail.name %>
  </div>
<% else %>
    <%= @cocktail.name %>
<% end %>

  <p><strong>Ingredients:</strong></p>
    <ul>
      <% @cocktail.doses.each do |dose|%>
        <li>
          <%= dose.description %>
          <%= dose.ingredient.name %>
          <%= link_to "🗑 Delete", dose_path(dose), method: :delete %>
        </li>
      <% end %>
    </ul>

  <button type="button" class="btn btn-light p-3"><%= link_to "Add new ingredient", new_cocktail_dose_path(@cocktail) %></button>
  <br>
  <br>
  <button type="button" class="btn btn-light p-3"><%= link_to 'Back', root_path %></button>
  <br>

</div>
